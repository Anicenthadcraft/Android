<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.ancient.ancient_handcraft">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <application
            android:name=".AncientHandcraftApplication"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".feature.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".feature.Login.LoginActivity"></activity>
        <activity android:name=".feature.SignUp.SignUpActivity"></activity>
        <activity android:name=".feature.ForgotPassword.ForgotPasswordActivity"></activity>

        <activity android:name=".base.Activity.Dashboard.DashboardActivity"></activity>
    </application>

</manifest>